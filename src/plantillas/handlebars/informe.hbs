<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Informe de Reservas</title>
    <style>
        body {
            font-family: 'Roboto', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 13px;
            margin: 40px;
            color: #2c3e50;
            background-color: #f4f6f9;
        }

        h1 {
            text-align: center;
            color: #34495e;
            border-bottom: 3px solid #1abc9c;
            padding-bottom: 10px;
            margin-bottom: 30px;
        }

        h2 {
            color: #1abc9c;
            margin-top: 40px;
            margin-bottom: 10px;
            font-size: 16px;
            border-left: 5px solid #1abc9c;
            padding-left: 10px;
        }

        .fecha {
            text-align: right;
            font-size: 11px;
            color: #7f8c8d;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
            margin-top: 10px;
            background: #fff;
            border-radius: 6px;
            overflow: hidden;
            box-shadow: 0 2px 6px rgba(0,0,0,0.05);
        }

        th {
            background-color: #1abc9c;
            color: #fff;
            padding: 10px;
            text-align: left;
            font-weight: 600;
            font-size: 13px;
        }

        td {
            padding: 10px;
            border-bottom: 1px solid #ecf0f1;
            font-size: 12px;
        }

        tr:last-child td {
            border-bottom: none;
        }

        tr:nth-child(even) {
            background-color: #f9fdfd;
        }

        .footer {
            text-align: center;
            font-size: 11px;
            margin-top: 50px;
            color: #95a5a6;
        }
    </style>
</head>
<body>
    <h1>📅 Informe de Reservas</h1>
    <p class="fecha">Generado el: {{fechaGeneracion}}</p>

    <h2>📋 Detalle de Reservas</h2>
    <table>
        <thead>
            <tr>
                <th>Fecha Reserva</th>
                <th>Salón</th>
                <th>Turno</th>
            </tr>
        </thead>
        <tbody>
            {{#each reservas}}
            <tr>
                <td>{{this.fecha_reserva}}</td>
                <td>{{this.titulo}}</td>
                <td>{{this.orden}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <h2>📍 Totales por Salón</h2>
    <table>
        <thead>
            <tr>
                <th>Salón</th>
                <th>Cantidad de Reservas</th>
                <th>Total Ingresos</th>
            </tr>
        </thead>
        <tbody>
            {{#each total}}
            <tr>
                <td>{{this.titulo}}</td>
                <td>{{this.cantidad_reservas}}</td>
                <td>${{this.total_ingresos}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <h2>📆 Ingresos por Mes</h2>
    <table>
        <thead>
            <tr>
                <th>Mes</th>
                <th>Ingresos</th>
            </tr>
        </thead>
        <tbody>
            {{#each mes}}
            <tr>
                <td>{{this.mes}}</td>
                <td>${{this.ingresos}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <p class="footer">Programación III - 2025</p>
</body>
</html>


{{!-- <!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Informe de Reservas</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 12px;
            margin: 40px;
            color: #333;
        }

        h1 {
            text-align: center;
            color: #2c3e50;
            border-bottom: 2px solid #2c3e50;
            padding-bottom: 10px;
        }

        h2 {
            color: #2c3e50;
            margin-top: 30px;
            border-bottom: 1px solid #ccc;
            padding-bottom: 5px;
        }

        .fecha {
            text-align: right;
            font-size: 10px;
            color: #555;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th {
            background-color: #2980b9;
            color: #fff;
            padding: 8px;
            text-align: left;
        }

        td {
            border-bottom: 1px solid #ccc;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #f8f8f8;
        }

        .footer {
            text-align: center;
            font-size: 10px;
            margin-top: 40px;
            color: #777;
        }
    </style>
</head>
<body>
    <h1>📅 Informe de Reservas</h1>

    <h2>📋 Detalle de Reservas</h2>
    <table>
        <thead>
            <tr>
                <th>Fecha Reserva</th>
                <th>Salón</th>
                <th>Turno</th>
            </tr>
        </thead>
        <tbody>
            {{#each reservas}}
            <tr>
                <td>{{this.fecha_reserva}}</td>
                <td>{{this.titulo}}</td>
                <td>{{this.orden}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <h2>📍 Totales por Salón</h2>
    <table>
        <thead>
            <tr>
                <th>Salón</th>
                <th>Cantidad de Reservas</th>
                <th>Total Ingresos</th>
            </tr>
        </thead>
        <tbody>
            {{#each total}}
            <tr>
                <td>{{this.titulo}}</td>
                <td>{{this.cantidad_reservas}}</td>
                <td>${{this.total_ingresos}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <h2>📆 Ingresos por Mes</h2>
    <table>
        <thead>
            <tr>
                <th>Mes</th>
                <th>Ingresos</th>
            </tr>
        </thead>
        <tbody>
            {{#each mes}}
            <tr>
                <td>{{this.mes}}</td>
                <td>${{this.ingresos}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <p class="footer">Programación III - 2025</p>
</body>
</html> --}}